name: ninja_prime
description: Utility functions and methods to generate random BigInt, generate random prime BigInt and check if BigInt is prime.
version: 2.0.0
homepage: https://github.com/ninja-dart/prime

documentation:

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:

dev_dependencies:
  pedantic: ^1.11.1
  test: ^1.21.7
